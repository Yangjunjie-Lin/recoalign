# Experiment manifests

The canonical experiment record is generated at runtime by `recoalign init-run`. Keep manually
curated sweep definitions here only when they point to committed configurations and explicit
seed sets.
