experiment:
  name: synthetic_compositional_world
  seed: 7
  output_dir: results/synthetic_compositional_world

synthetic:
  count: 30
  image_size: 128
  write_images: true

model:
  backend: reference
  seed: 7
  checkpoint_manifest: manifests/checkpoints/synthetic_ci.yaml

evaluation:
  bootstrap_samples: 1000
  conditions: [image_only, object_list, caption, scene_graph]
