# Validation Records

Canonical machine-readable validation records are stored in `release/validation/` so they can be
referenced independently of the packaged documentation. Release-asset checksums are stored in
`../checksums/SHA256SUMS` after deterministic archive construction.
