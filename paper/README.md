# Paper workspace

This directory is reserved for manuscript-controlled assets rather than raw experiment outputs.

**Current gate: paper writing is blocked.** The original hypothesis is falsified and the selected
pivot hypothesis has not been preregistered or tested. Existing figures/tables are retained audit
assets and must not be assembled into a manuscript narrative.

Suggested structure:

```text
paper/
├── figures/
├── tables/
├── sections/
└── references/
```

Figures and tables should be generated from committed scripts or lightweight result manifests whenever possible. Do not manually transcribe headline metrics from terminal output.
