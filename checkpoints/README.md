# Local checkpoint cache

Checkpoint bytes remain outside Git. Use the manifests under `manifests/checkpoints/` to bind a
model identifier, revision, license, and optional local file hashes to a run.
