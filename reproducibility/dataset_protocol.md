# Dataset protocol

Dataset versions, split rules, manifests, and leakage assertions are registered under `research/experiments/` and `manifests/datasets/`. The frozen benchmark matrix disallows test-set tuning and split overrides.
