# Results boundary

No PIVOT_EXP_A3 model result exists at preregistration time. This directory will receive only the
registered result assets after every pre-inference barrier in `config.yaml` passes. Development
checks, tokenizer inspection, parser tests, and synthetic image generation are not scientific model
results.
