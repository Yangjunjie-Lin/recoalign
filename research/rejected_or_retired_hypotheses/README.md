# Rejected or retired hypothesis archive

These lifecycle records close the current ReCoAlign hypothesis chain. They are provenance, not a
candidate backlog. H002 is recorded in `research/current_line_closeout.yaml` as
`supported_in_frozen_LLaVA_scope_only` and is not generalized beyond that scope.

