# Real-VLM failure analysis

`recoalign run-vlm-eval` writes immutable per-run error rows and summaries here, grouped by
experiment, model, and run directory. The registered categories are object failure, relation failure,
multi-hop failure, and structure misuse.
