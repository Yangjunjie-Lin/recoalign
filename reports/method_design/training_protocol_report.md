# ReCoAlign training protocol report

The proposed three stages are interface pretraining, instruction adaptation, and
optional task fine-tuning.  The repository currently validates stage-1 mechanics
with a CPU toy world; stages 2–3 require a selected real VLM checkpoint and are not
claimed as completed.
