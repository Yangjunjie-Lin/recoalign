# Representation analysis report

The implementation compares visual-token and structure-token linear probes for
object, attribute, relation, and composition labels. It also produces cosine
similarity matrices, deterministic clusters, and reasoning-attention mass over
structure slots.

Probe accuracy, clustering, or attention alone is not treated as causal evidence;
they are reported alongside token interventions and paired benchmark effects.
